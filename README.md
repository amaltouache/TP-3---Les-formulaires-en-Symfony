# TP-3---Les-formulaires-en-Symfony
Le but de ce TP est de construire le formulaire

1.étapes réalisés :
-Clonage du projet empty-symfony-project
-Configuration de l'environnement 


2.Travaux réalisés:
-Création du Form Type:(Quantity/color)
-Personnalisation des champs 
-Création du controller (route/ définit pour afficher le formulaire)
-Formulaire géré avec [(crearform(ProductType::classh) handleRequest($request)]
-Passage de la vue du formulaire twig avc[$form->createView()]
-Création du template Twig index.html.twig (Page produit avec images,détailset formulaire )
-Formulaires affiché avec la méthode granulaires [form_start()/form_widget()/form_label()/form-end()]
